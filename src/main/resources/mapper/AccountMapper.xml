<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.trade.folio.domain.account.mapper.AccountMapper">

    <select id="findByMemberId" resultType="com.trade.folio.domain.account.entity.Account">
        SELECT id, member_id, account_name, broker, currency, balance,
               updated_at, created_at
          FROM folio.account
         WHERE member_id = #{memberId}
         ORDER BY created_at
    </select>

    <select id="findById" resultType="com.trade.folio.domain.account.entity.Account">
        SELECT id, member_id, account_name, broker, currency, balance,
               updated_at, created_at
          FROM folio.account
         WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO folio.account (member_id, account_name, broker, currency)
        VALUES (#{memberId}, #{accountName}, #{broker}, #{currency})
    </insert>

    <select id="countByMemberId" resultType="int">
        SELECT COUNT(*)
          FROM folio.account
         WHERE member_id = #{memberId}
    </select>

    <select id="countByMemberIdAndAccountName" resultType="int">
        SELECT COUNT(*)
          FROM folio.account
         WHERE member_id = #{memberId}
           AND account_name = #{accountName}
    </select>

    <update id="update" parameterType="com.trade.folio.domain.account.entity.Account">
        UPDATE folio.account
           SET account_name = #{accountName},
               broker       = #{broker},
               currency     = #{currency},
               updated_at   = NOW()
         WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM folio.account
         WHERE id = #{id}
    </delete>

</mapper>
